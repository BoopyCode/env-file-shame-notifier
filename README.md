# .env Shame Bell

Rings a bell when you try to commit .env files to git. Install as a git pre-commit hook: `cp env-shame-bell.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit`. Because apparently .gitignore needs backup.